<?xml version="1.0" encoding="UTF-8"?>

<!--
  Copyright (c) 2011 Cloudera, Inc. All rights reserved.  
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.cloudera</groupId>
    <artifactId>parent</artifactId>
    <version>1.0</version>
  </parent>
  <groupId>com.cloudera.impala</groupId>
  <artifactId>impala-frontend</artifactId>
  <version>0.1-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>Cloudera Impala Query Engine Frontend</name>

  <properties>
    <test.hive.testdata>${project.basedir}/../testdata/target/AllTypes.txt</test.hive.testdata>
    <test.hive.warehouse.dir>${project.build.directory}/test-warehouse</test.hive.warehouse.dir>
    <test.hive.metastore.jdbc.url>jdbc:derby:;databaseName=${project.build.directory}/test_metastore_db;create=true;logDevice=${project.build.directory}/test_metastore_db</test.hive.metastore.jdbc.url>
    <test.hive.metastore.jdbc.driver>org.apache.derby.jdbc.EmbeddedDriver</test.hive.metastore.jdbc.driver>
    <test.hive.metastore.jdbc.username>APP</test.hive.metastore.jdbc.username>
    <test.hive.metastore.jdbc.password>mine</test.hive.metastore.jdbc.password>

    <thrift.executable>/usr/local/bin/thrift</thrift.executable>
  </properties>
  
  <dependencies>
    <dependency>
      <groupId>net.sf.squirrel-sql.thirdparty-non-maven</groupId>
      <artifactId>java-cup</artifactId>
      <version>0.11a</version>
    </dependency>

    <dependency>
      <groupId>org.apache.hadoop.hive</groupId>
      <artifactId>hive-metastore</artifactId>
      <version>0.7.0-cdh3u0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop.hive</groupId>
      <artifactId>hive-common</artifactId>
      <version>0.7.0-cdh3u0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop.hive</groupId>
      <artifactId>hive-serde</artifactId>
      <version>0.7.0-cdh3u0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-core</artifactId>
      <version>0.20.2-cdh3u0</version>
    </dependency>
    <dependency>
      <groupId>beaver</groupId>
      <artifactId>beaver</artifactId>
      <version>0.9.7</version>
    </dependency>
    <dependency>
      <groupId>org.apache.thrift</groupId>
      <artifactId>libthrift</artifactId>
      <version>0.6.0-cdh</version>
    </dependency>
    <dependency>
      <groupId>org.datanucleus</groupId>
      <artifactId>datanucleus-connectionpool</artifactId>
      <version>2.0.3</version>
    </dependency>
    <dependency>
      <groupId>mysql</groupId>
      <artifactId>mysql-connector-java</artifactId>
      <version>5.1.9</version>
    </dependency>
    <dependency>
      <groupId>commons-dbcp</groupId>
      <artifactId>commons-dbcp</artifactId>
      <version>1.4</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop.hive</groupId>
      <artifactId>hive-shims</artifactId>
      <version>0.7.0-cdh3u0</version>
    </dependency>

    <dependency>
      <groupId>commons-cli</groupId>
      <artifactId>commons-cli</artifactId>
      <version>1.2</version>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>1.6.1</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <version>1.6.1</version>
    </dependency>

    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <version>r09</version>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.8.2</version>
      <scope>test</scope>
    </dependency>

  </dependencies>
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.3.2</version>
        <configuration>
          <source>1.6</source>
          <target>1.6</target>
        </configuration>
      </plugin>

      <plugin>
        <groupId>net.sourceforge.czt</groupId>
        <artifactId>maven-cup-plugin</artifactId>
        <version>1.6.4</version>
        <executions>
          <execution>
            <id>cup</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate</goal>
            </goals>
            <configuration>
              <extension>y</extension>
              <parserOutput>SqlParser</parserOutput>
              <symbolOutput>SqlParserSymbols</symbolOutput>
	      <outputDirectory>${project.build.directory}/generated-sources/cup</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>de.jflex</groupId>
        <artifactId>maven-jflex-plugin</artifactId>
        <version>1.4.3</version>
        <executions>
          <execution>
            <id>jflex</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate</goal>
            </goals>
            <configuration>
              <backup>false</backup>
            </configuration>
          </execution>
        </executions>
      </plugin>
<!-- PDH - I don't think we need this right now, regardless there's a better way (ie not part of dev workflow)
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <configuration>
          <descriptorRefs>
            <descriptorRef>jar-with-dependencies</descriptorRef>
          </descriptorRefs>
        </configuration>
        <executions>
          <execution>
            <id>make-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
-->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>2.3</version>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
   
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.7.2</version>
        <configuration>
          <redirectTestOutputToFile>true</redirectTestOutputToFile>
          <systemPropertyVariables>
            <test.hive.warehouse.dir>${test.hive.warehouse.dir}</test.hive.warehouse.dir>
            <test.hive.metastore.jdbc.url>${test.hive.metastore.jdbc.url}</test.hive.metastore.jdbc.url>
            <test.hive.metastore.jdbc.driver>${test.hive.metastore.jdbc.driver}</test.hive.metastore.jdbc.driver>
            <test.hive.metastore.jdbc.username>${test.hive.metastore.jdbc.username}</test.hive.metastore.jdbc.username>
            <test.hive.metastore.jdbc.password>${test.hive.metastore.jdbc.password}</test.hive.metastore.jdbc.password>
	    <java.library.path>${java.library.path}:${project.basedir}/../be/build/service</java.library.path>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <plugin>
        <groupId>com.atlassian.maven.plugins</groupId>
        <artifactId>maven-clover2-plugin</artifactId>
        <version>3.0.2</version>
        <configuration>
          <generateHtml>true</generateHtml>
          <generateXml>true</generateXml>
        </configuration>
      </plugin>

      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>generate-thrift-sources</id>
            <phase>generate-sources</phase>
            <configuration>
              <tasks>
                <apply executable="${thrift.executable}" force="true" type="file">
                  <arg value="--gen" />
                  <arg value="java:hashcode"/>
                  <arg value="-o"/>
                  <arg value="${project.build.directory}/generated-sources"/>
                  <fileset dir="${project.basedir}/src/main/thrift">
                    <include name="**/*.thrift"/>
                  </fileset>
                </apply>
                <apply executable="${thrift.executable}" force="true" type="file">
                  <arg value="--gen" />
                  <arg value="cpp"/>
                  <arg value="-o"/>
                  <arg value="${project.basedir}/../be/generated-sources"/>
                  <fileset dir="${project.basedir}/src/main/thrift">
                    <include name="**/*.thrift"/>
                  </fileset>
                </apply>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>1.5</version>
        <executions>
          <!-- Tell maven about our generated files -->
          <execution>
            <id>add-source</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>add-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>${project.build.directory}/generated-sources/gen-java</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

         <plugin> 
             <groupId>org.codehaus.mojo</groupId> 
             <artifactId>exec-maven-plugin</artifactId> 
             <executions> 
                 <execution> 
                     <goals> 
                         <goal>java</goal> 
                     </goals> 
                 </execution> 
             </executions> 
             <configuration> 
<!--
                 <mainClass>com.cloudera.impala.service.Coordinator</mainClass> 
-->
                 <systemProperties> 
		    <systemProperty>
		       <key>java.library.path</key>
		       <value>${java.library.path}:${project.basedir}/../be/build/service</value>
		    </systemProperty>
                     <systemProperty> 
                       <key>test.hive.testdata</key>
		       <value>${project.basedir}/../testdata/target/AllTypes.txt</value> 
		     </systemProperty>
		     <systemProperty>
		       <key>test.hive.warehouse.dir</key>
		       <value>${project.build.directory}/test-warehouse</value>
		     </systemProperty>
		     <systemProperty>
		       <key>test.hive.metastore.jdbc.url</key>
		       <value>jdbc:derby:;databaseName=${project.build.directory}/test_metastore_db;create=true;logDevice=${project.build.directory}/test_metastore_db</value>
		     </systemProperty>
		     <systemProperty>
		       <key>test.hive.metastore.jdbc.driver</key>
		       <value>org.apache.derby.jdbc.EmbeddedDriver</value>
		     </systemProperty>
		     <systemProperty>
		       <key>test.hive.metastore.jdbc.username</key>
		       <value>APP</value>
		     </systemProperty>
		     <systemProperty>
		       <key>test.hive.metastore.jdbc.password</key>
		       <value>mine</value>
                     </systemProperty> 
                 </systemProperties> 
             </configuration> 
         </plugin> 
    </plugins>
  </build>
              
  <repositories>
    <repository>
      <id>cdh.releases.repo</id>
      <url>https://repository.cloudera.com/content/repositories/releases</url>
      <name>CDH Releases Repository</name>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>cdh.snapshots.repo</id>
      <url>https://repository.cloudera.com/content/repositories/snapshots</url>
      <name>CDH Snapshots Repository</name>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>cloudera.thirdparty.repo</id>
      <url>https://repository.cloudera.com/content/repositories/third-party</url>
      <name>Cloudera Third Party Repository</name>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>cloudera.thirdparty.repo</id>
      <url>https://repository.cloudera.com/content/repositories/third-party</url>
      <name>Cloudera Third Party Repository</name>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </pluginRepository>
    <pluginRepository>
      <id>cloudera.snapshot.repo</id>
      <url>https://repository.cloudera.com/content/repositories/snapshots</url>
      <name>Cloudera Snapshot Repository</name>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </pluginRepository>

    <pluginRepository>
      <id>dtrott</id>
      <url>http://maven.davidtrott.com/repository</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </pluginRepository>
  </pluginRepositories>

  <profiles>
    <profile>
      <!-- -->
      <id>load-testdata</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.6</version>
            <executions>
              <execution>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <target>
                    <exec executable="/bin/bash">
                      <arg value="${project.basedir}/../testdata/bin/run-hive.sh"/>
                      <arg value="${project.basedir}/src/test/resources"/>
                      <arg value="${project.basedir}/../testdata/bin/create.sql"/>
                      <arg value="${test.hive.warehouse.dir}"/>
                      <arg value="${test.hive.metastore.jdbc.url}"/>
                      <arg value="${test.hive.metastore.jdbc.driver}"/>
                      <arg value="${test.hive.metastore.jdbc.username}"/>
                      <arg value="${test.hive.metastore.jdbc.password}"/>
                    </exec>
                    <exec executable="/bin/bash">
                      <arg value="${project.basedir}/../testdata/bin/run-hive.sh"/>
                      <arg value="${project.basedir}/src/test/resources"/>
                      <arg value="${project.basedir}/../testdata/bin/load.sql"/>
                      <arg value="${test.hive.warehouse.dir}"/>
                      <arg value="${test.hive.metastore.jdbc.url}"/>
                      <arg value="${test.hive.metastore.jdbc.driver}"/>
                      <arg value="${test.hive.metastore.jdbc.username}"/>
                      <arg value="${test.hive.metastore.jdbc.password}"/>
                    </exec>
                  </target>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>thrift-home-defined</id>
      <activation>
        <file>
          <exists>${env.THRIFT_HOME}/bin/thrift</exists>
          </file>
      </activation>
      <properties>
        <thrift.executable>${env.THRIFT_HOME}/bin/thrift</thrift.executable>
      </properties>
    </profile>
  </profiles>

</project>
